export const CCD_STEPS = 2;
export const MAX_SPEED = 10;
/** Minimum speed before velocity is considered 0 */
export const MIN_SPEED = 0.03;

/** When rendering the lines, scale up the apparent speed */
export const VISUAL_SPEED = 30;
export const MIN_INPUT_SPEED = 20;

/** Scale up so ASPECT_WIDTH takes up as much width as possible */
export const ASPECT_WIDTH = 500;
export const ASPECT_HEIGHT = 500;